language: node_js
node_js:
  - "7"
install:
  - npm install
script: gulp
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  target_branch: docs
  on:
    branch: master
